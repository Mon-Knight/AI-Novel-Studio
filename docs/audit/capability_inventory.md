# AI Novel Studio 全量能力清单 (Capability Inventory)

> **审计日期**：2026-08-23  
> **审计基准版本**：v3.6.0 (架构演进基线 v3.5.0)  
> **审计范围**：`src/`（Pages, Components, Features, Services, Agent, Agent-Tools, Hooks, Prompts）、`src-tauri/`（Commands, Migrations, Repositories, Services, AI Gateway, Large Text Engine）、SQLite Schema (36 项迁移)、AI 调用点及端到端创作链路。  
> **状态口径**：
>
> - `WORKING`：完整链路实际代码验证 + 测试用例通过
> - `PARTIAL`：部分链路可用（如仅 LocalStorage 或仅内存原型）
> - `BROKEN`：代码断链或已被废弃脱节
> - `UNKNOWN`：无法可靠验证（本次审计已实现 100% 确认，无 UNKNOWN）

---

## 汇总统计

| 指标                            | 数量      | 占比  | 说明                                                             |
| :------------------------------ | :-------- | :---- | :--------------------------------------------------------------- |
| **独立能力总数**                | **96 项** | 100%  | 覆盖 14 个领域模块                                               |
| **WORKING (完全可用)**          | **88 项** | 91.7% | 具备完整 UI/Service/Command/DB 闭环并通过测试                    |
| **PARTIAL (部分可用/双轨原型)** | **6 项**  | 6.2%  | 模板中心(仅LS)、In-Memory Agent、v2.0 Placement、MemoryManager等 |
| **BROKEN (已废弃/断链代码)**    | **1 项**  | 1.0%  | `src/agent/planner-lite.ts` 早期孤立代码                         |
| **SHELL (纯占位壳子)**          | **1 项**  | 1.0%  | `ComingSoonPage` 路由占位                                        |
| **UNKNOWN (未明确)**            | **0 项**  | 0%    | 全部完成三向交叉核对                                             |

---

## 全量能力明细总表

| capability_id    | 能力名称                                      | 当前入口                                                   | 完整调用链                                                                                                                                                                                                                | 输入 / 输出                                                       | 是否修改数据 | 是否需要模型 | 当前使用            | 可用性                   | Tool候选 | SubAgent候选    | 重复实现                                       | 失败点/风险                                                 | 优先级 |
| :--------------- | :-------------------------------------------- | :--------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------- | :----------- | :----------- | :------------------ | :----------------------- | :------- | :-------------- | :--------------------------------------------- | :---------------------------------------------------------- | :----- |
| **CAP-NOV-001**  | 获取小说作品列表                              | HomePage (`/novels`)                                       | `HomePage` → `novelRepository.getAll()` → `invoke('get_all_novels')` → `novel_repo.get_all()` → SQLite `novels`                                                                                                           | In: 无<br>Out: `Novel[]`                                          | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 软删除记录过滤                                              | P0     |
| **CAP-NOV-002**  | 创建小说作品                                  | HomePage 创建弹窗                                          | `NovelFormModal` → `novelRepository.create()` → `invoke('create_novel')` → SQLite `novels`                                                                                                                                | In: `CreateNovelInput`<br>Out: `Novel`                            | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 标题为空校验                                                | P0     |
| **CAP-NOV-003**  | 获取作品详情                                  | NovelDetailPage (`/novels/:id`)                            | `NovelDetailPage` → `novelRepository.getById()` → `invoke('get_novel_by_id')` → SQLite `novels`                                                                                                                           | In: `novelId`<br>Out: `Novel \| null`                             | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 作品不存在时返回 null                                       | P0     |
| **CAP-NOV-004**  | 更新作品基础信息                              | NovelDetailPage 编辑表单                                   | `NovelDetailPage` → `novelRepository.update()` → `invoke('update_novel')` → SQLite `novels`                                                                                                                               | In: `UpdateNovelInput`<br>Out: `Novel`                            | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 字段校验                                                    | P1     |
| **CAP-NOV-005**  | 删除作品 (级联软删除)                         | HomePage / NovelDetailPage                                 | `NovelDetailPage` → `novelRepository.delete()` → `invoke('delete_novel')` → SQLite `novels` (set `deleted_at`)                                                                                                            | In: `novelId`<br>Out: `void`                                      | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 级联草稿清理                                                | P1     |
| **CAP-NOV-006**  | 世界背景设定 CRUD                             | NovelDetailPage 世界观卡片                                 | `WorldSettingModal` → `worldSettingRepository` → `invoke('get_world_settings'/'save_world_setting')` → SQLite `world_settings`                                                                                            | In: `WorldSettingInput`<br>Out: `WorldSetting`                    | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 StoryAssets 部分重叠                        | JSON 解析错误                                               | P1     |
| **CAP-NOV-007**  | 规则体系设定 CRUD                             | NovelDetailPage 规则卡片                                   | `RuleSystemModal` → `ruleSystemRepository` → `invoke('get_rule_systems'/'save_rule_system'/'delete_rule_system')` → SQLite `rule_systems`                                                                                 | In: `RuleSystemInput`<br>Out: `RuleSystem`                        | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 必填项缺失                                                  | P1     |
| **CAP-NOV-008**  | 主角设定管理 (单/双主角)                      | NovelDetailPage 主角卡片                                   | `ProtagonistModal` → `protagonistRepository` → `invoke('get_protagonist'/'save_protagonist')` → SQLite `protagonists` + `novels`                                                                                          | In: `ProtagonistInput`<br>Out: `Protagonist`                      | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 Characters 库双写镜像                       | 双写一致性需同步                                            | P0     |
| **CAP-NOV-009**  | 主角自动同步至角色库                          | 主角保存时自动触发                                         | `protagonistRepository.save()` → `invoke('sync_protagonist_to_character_library')` → SQLite `characters`                                                                                                                  | In: `novelId, protagonist`<br>Out: `void`                         | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 characterService 重叠                       | 唯一索引冲突保护                                            | P1     |
| **CAP-CHAP-001** | 分卷与章节树检索                              | WritingWorkspace (`/novels/:id/workspace`)                 | `VolumeTree` → `volumeService.listVolumesByNovelId` + `chapterService.listChaptersByNovelId` → `invoke('get_volumes_by_novel_id'/'get_chapters_by_novel_id')` → SQLite `volumes, chapters`                                | In: `novelId`<br>Out: `{ volumes, chapters }`                     | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 卷章排序索引                                                | P0     |
| **CAP-CHAP-002** | 创建分卷                                      | WritingWorkspace VolumeTree                                | `VolumeTreeDialogs` → `volumeService.createVolume` → `invoke('create_volume')` → SQLite `volumes`                                                                                                                         | In: `CreateVolumeInput`<br>Out: `Volume`                          | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 顺序号计算                                                  | P1     |
| **CAP-CHAP-003** | 更新/删除分卷                                 | WritingWorkspace VolumeTree                                | `VolumeTree` → `volumeService.updateVolume/deleteVolume` → `invoke('update_volume'/'delete_volume')` → SQLite `volumes`                                                                                                   | In: `volumeId, input`<br>Out: `void`                              | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 包含子章节时拒绝删除                                        | P1     |
| **CAP-CHAP-004** | 创建章节                                      | WritingWorkspace VolumeTree                                | `VolumeTreeDialogs` → `chapterService.createChapter` → `invoke('create_chapter')` → SQLite `chapters`                                                                                                                     | In: `CreateChapterInput`<br>Out: `Chapter`                        | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | chapterNumber 自动递增                                      | P0     |
| **CAP-CHAP-005** | 更新章节大纲与状态                            | WritingWorkspace EditorArea                                | `EditorArea` → `chapterService.updateChapter` → `invoke('update_chapter')` → SQLite `chapters`                                                                                                                            | In: `chapterId, fields`<br>Out: `Chapter`                         | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 状态机跃迁约束                                              | P0     |
| **CAP-CHAP-006** | 删除章节                                      | WritingWorkspace VolumeTree                                | `VolumeTree` → `chapterService.deleteChapter` → `invoke('delete_chapter')` → SQLite `chapters`                                                                                                                            | In: `chapterId`<br>Out: `void`                                    | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 草稿级联处理                                                | P1     |
| **CAP-CHAP-007** | 读取已采用章节正文                            | EditorArea / 生成管线                                      | `draftVersionService.getAdoptedByChapterId()` → `invoke('get_adopted_draft_by_chapter_id'/'read_chapter_draft_content')` → SQLite `chapter_drafts` + Large Text Document                                                  | In: `chapterId`<br>Out: `ChapterDraft \| null`                    | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 draftService.getLatest 重叠                 | 大文本分片丢失熔断                                          | P0     |
| **CAP-CHAP-008** | 查询章节草稿历史列表                          | RightDock DraftHistoryPanel                                | `DraftHistoryPanel` → `draftService.getByChapterId()` → `invoke('get_drafts_by_chapter_id')` → SQLite `chapter_drafts`                                                                                                    | In: `chapterId`<br>Out: `ChapterDraft[]`                          | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 分页与大草稿字数统计                                        | P1     |
| **CAP-CHAP-009** | 原子保存草稿 (CAS Hash 校验)                  | EditorArea (手动/自动保存)                                 | `EditorArea` → `draftVersionService.saveDraftAtomic()` → `invoke('save_chapter_draft_atomic')` → Rust `draft_service::save_draft_atomic` (SHA-256 CAS) → SQLite `chapter_drafts`                                          | In: `AtomicSaveInput`<br>Out: `ChapterDraft`                      | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 largeTextSave 配合                          | `DOCUMENT_HASH_MISMATCH` 冲突保护                           | P0     |
| **CAP-CHAP-010** | 采用指定草稿版本                              | EditorArea / 候选审查卡片                                  | `EditorArea` → `draftVersionService.adoptDraft()` → `invoke('adopt_chapter_draft')` → Rust transaction → SQLite `chapters.adopted_draft_id` + `memory_documents` 失效                                                     | In: `chapterId, draftId`<br>Out: `void`                           | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 自动触发 Memory 向量失效                                    | P0     |
| **CAP-CHAP-011** | 删除草稿版本                                  | RightDock DraftHistoryPanel                                | `DraftHistoryPanel` → `draftVersionService.deleteDraft()` → `invoke('delete_chapter_draft')` → SQLite `chapter_drafts`                                                                                                    | In: `draftId`<br>Out: `void`                                      | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 禁止删除已采用草稿                                          | P2     |
| **CAP-CHAP-012** | 大文本分片流式存储与完整性校验                | 正文超过 64KB 保存时自动触发                               | `largeTextSave.ts` → `create_large_text_save_session` → `append_large_text_chunk` → `finalize_large_text_save` → SQLite `large_text_documents/chunks`                                                                     | In: `content, chunkSize`<br>Out: `largeTextRefId, sha256`         | 是           | 否           | 是                  | **WORKING**              | 否       | 否              | 无                                             | 分片篡改时 fail-closed                                      | P0     |
| **CAP-CHAP-013** | 工作台崩溃/刷新恢复快照                       | EditorArea 实时编辑                                        | `useEditorDocumentController` → `workspaceRecoveryService.upsertSnapshot` → `invoke('upsert_workspace_recovery_snapshot')` → SQLite `workspace_recovery_snapshots`                                                        | In: `RecoverySnapshot`<br>Out: `void`                             | 是           | 否           | 是                  | **WORKING**              | 否       | 否              | 无                                             | 本地草稿版本过期比对                                        | P0     |
| **CAP-OUT-001**  | 作品总大纲管理与版本切换                      | OutlineEditorPage (`/novels/:id/outline`)                  | `OutlineEditorPage` → `outlineService` → `invoke('save_master_outline'/'get_master_outline'/'set_active_master_outline')` → SQLite `master_outlines`                                                                      | In: `outlineContent, version`<br>Out: `MasterOutline`             | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 版本分叉                                                    | P1     |
| **CAP-OUT-002**  | 分卷大纲管理与版本切换                        | OutlineEditorPage                                          | `OutlineEditorPage` → `outlineService` → `invoke('save_volume_outline'/'get_volume_outline'/'set_active_volume_outline')` → SQLite `volume_outlines`                                                                      | In: `volumeId, content`<br>Out: `VolumeOutline`                   | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 卷绑定有效性                                                | P1     |
| **CAP-OUT-003**  | 章节大纲管理与版本切换                        | OutlineEditorPage / RightDock OutlinePanel                 | `OutlinePanel` → `outlineService` → `invoke('save_chapter_outline'/'get_chapter_outline'/'set_active_chapter_outline')` → SQLite `chapter_outlines`                                                                       | In: `chapterId, content`<br>Out: `ChapterOutline`                 | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 章节同步                                                    | P1     |
| **CAP-OUT-004**  | 构建三级大纲上下文                            | AI 生成前上下文装配                                        | `promptBuilder` → `outlineService.buildOutlineContext` → `invoke('build_outline_context')` → Rust `outline_commands::build_outline_context`                                                                               | In: `novelId, chapterId`<br>Out: `OutlineContextString`           | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 上下文 Token 预算溢出截断                                   | P0     |
| **CAP-OUT-005**  | AI 大纲自动生成与扩写                         | OutlineEditorPage AI 按钮                                  | `OutlineEditorPage` → `outlineGenerateService.generateOutline()` → `aiExecutionPipeline` → AI Model API                                                                                                                   | In: `OutlineGenerateParams`<br>Out: `GeneratedOutline`            | 否           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 DSH candidate outline 重叠                  | JSON 解析格式异常                                           | P1     |
| **CAP-STY-001**  | 写作风格方案 CRUD 与激活                      | StyleProfilesPage (`/styles`) / RightDock StylePanel       | `StyleProfilesPage` → `styleProfileService` → `invoke('list_style_profiles'/'save_style_profile'/'set_active_style_profile')` → SQLite `style_profiles`                                                                   | In: `StyleProfileInput`<br>Out: `StyleProfile`                    | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 跨作品方案复用                                              | P1     |
| **CAP-STY-002**  | 章节输出控制方案 CRUD 与默认值                | StyleProfilesPage                                          | `StyleProfilesPage` → `outputProfileService` → `invoke('list_output_profiles'/'save_output_profile'/'set_default_output_profile')` → SQLite `output_profiles`                                                             | In: `OutputProfileInput`<br>Out: `OutputProfile`                  | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 字数区间约束逻辑                                            | P1     |
| **CAP-STY-003**  | 样本文本 AI 风格画像分析                      | StyleProfilesPage 文本分析弹窗                             | `StyleProfileDialogs` → `styleAnalyzeService.analyzeStyle()` → `layeredStyleAnalyzer` → AI Model API                                                                                                                      | In: `sampleText, title`<br>Out: `StyleDimensionsAnalysis`         | 否           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 无                                             | 样本文本过短或无风格特征                                    | P1     |
| **CAP-STY-004**  | 风格画像生成提示词投影                        | 正文生成管线上下文编译                                     | `generationContextCompiler` → `styleProfilePromptProjection.projectStyleProfileToPrompt()`                                                                                                                                | In: `StyleProfile, OutputProfile`<br>Out: `PromptDirectives`      | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 模板变量插值缺失                                            | P0     |
| **CAP-REF-001**  | 参考书导入与多格式解析 (TXT/EPUB/PDF/DOCX)    | ReferenceLibraryPage (`/novels/:id/references`)            | `ReferenceLibraryPage` → `referenceLibraryService.commitImport()` → `referenceTextParser` → `invoke('commit_reference_import')` → SQLite `reference_works/sections`                                                       | In: `filePath, novelId`<br>Out: `ReferenceWork`                   | 是           | 否           | 是                  | **WORKING**              | 否       | 否              | 无                                             | 编码检测异常与大文件分段                                    | P1     |
| **CAP-REF-002**  | 参考资料重复导入检测                          | ReferenceLibraryPage 导入前检查                            | `ReferenceLibraryPage` → `referenceLibraryService` → `invoke('inspect_reference_duplicates')` → SQLite `reference_duplicate_decisions`                                                                                    | In: `novelId, fileHash`<br>Out: `DuplicateInspectionResult`       | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 哈希相同但内容微调                                          | P1     |
| **CAP-REF-003**  | 参考书分卷章节浏览与只读切片                  | ReferenceLibraryPage 正文阅读器                            | `ReferenceLibraryPage` → `referenceLibraryService.getSectionContent()` → `invoke('get_reference_section_content')` → Large Text Engine                                                                                    | In: `sectionId`<br>Out: `string`                                  | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | UTF-16 标量边界越界保护                                     | P1     |
| **CAP-REF-004**  | 激活/删除参考书                               | ReferenceLibraryPage                                       | `ReferenceLibraryPage` → `referenceLibraryService` → `invoke('activate_reference_import'/'delete_reference_work')` → SQLite                                                                                               | In: `workId`<br>Out: `void`                                       | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 关联风格方案孤儿保护                                        | P2     |
| **CAP-REF-005**  | 参考书智能风格提取                            | ReferenceLibraryPage "提取风格"                            | `ReferenceLibraryPage` → `referenceStyleProfileService.extractStyleFromReference()` → `layeredStyleAnalyzer` → AI Model API                                                                                               | In: `workId, sectionIds`<br>Out: `StyleProfile`                   | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 styleAnalyzeService 重叠                    | 章节选取偏差导致画像失真                                    | P1     |
| **CAP-AST-001**  | 势力组织资产管理 (Faction)                    | StoryAssetsPage (`/novels/:id/story-assets`)               | `StoryAssetForms` → `contentTransactionService` → `invoke('list_faction_assets'/'apply_content_transaction')` → SQLite `story_factions`                                                                                   | In: `FactionInput`<br>Out: `StoryFaction`                         | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 势力名称重复                                                | P1     |
| **CAP-AST-002**  | 地理环境与层级架构 (Location)                 | StoryAssetsPage                                            | `StoryAssetForms` → `contentTransactionService` → `invoke('list_location_assets'/'apply_content_transaction')` → SQLite `story_locations`                                                                                 | In: `LocationInput`<br>Out: `StoryLocation`                       | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 父子层级循环依赖                                            | P1     |
| **CAP-AST-003**  | 跨章事务候选准备 (CAS 隔离)                   | StoryAssetsPage 跨章变更面板                               | `CrossChapterBatchPanel` → `contentTransactionService.prepareTransaction()` → `invoke('prepare_content_transaction')` → SQLite `content_transactions/prepared_candidates`                                                 | In: `novelId, targetChanges`<br>Out: `TransactionProposal`        | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 placement_proposals 双轨                    | 目标版本 CAS 不匹配                                         | P1     |
| **CAP-AST-004**  | 跨章多实体事务原子提交                        | StoryAssetsPage 审查面板                                   | `TransactionReview` → `contentTransactionService.applyTransaction()` → `invoke('apply_content_transaction')` → SQLite All-or-Nothing Commit                                                                               | In: `transactionId, planHash`<br>Out: `ApplyResult`               | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 artifactApply 重叠                          | 单实体冲突导致全局回滚                                      | P1     |
| **CAP-GEN-001**  | 章节工程化状态生命周期 (Draft/Active)         | RightDock ChapterEngineeringPanel                          | `ChapterEngineeringPanel` → `chapterEngineeringService` → `invoke('get_chapter_engineering_states'/'save_chapter_engineering_draft'/'activate_chapter_engineering_state')` → SQLite `chapter_engineering_states`          | In: `chapterId, stateJson`<br>Out: `EngineeringState`             | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 未激活状态阻断生成                                          | P0     |
| **CAP-GEN-002**  | 章节分镜卡与 Beat 节奏序列管理                | ChapterEngineeringPanel                                    | `ChapterEngineeringScenePlanView` → `chapterEngineeringService` + `chapterScenePlanService`                                                                                                                               | In: `ScenePlanJson`<br>Out: `ScenePlan`                           | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | Beat 顺序错乱                                               | P0     |
| **CAP-GEN-003**  | AI 分镜与 Beat 节奏规划生成                   | ChapterEngineeringPanel "AI 生成分镜"                      | `ChapterEngineeringPanel` → `chapterScenePlanService.generateScenePlan()` → `aiExecutionPipeline` → AI Model API                                                                                                          | In: `ChapterInfo, Outlines`<br>Out: `ScenePlanCandidate`          | 否           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 AgentTool generate_scene_plan 重叠          | JSON 解析结构不合规                                         | P0     |
| **CAP-GEN-004**  | 正文生成上下文编译与快照存证                  | 正文生成前自动调用                                         | `useChapterGenerationAction` → `generationContextCompiler.compile()` → `invoke('save_chapter_generation_snapshot')` → SQLite `chapter_generation_snapshots`                                                               | In: `chapterId, settings`<br>Out: `CompiledContextSnapshot`       | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 来源哈希校验                                                | P0     |
| **CAP-GEN-005**  | 生成作业状态机与崩溃断点恢复                  | AiGeneratePanel 启动任务                                   | `generationJobService.createJob()` → `jobStateMachine.transition()` → `checkpointRecovery` → SQLite `generation_jobs/steps`                                                                                               | In: `JobParams`<br>Out: `JobRecord`                               | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 Workbench task_runs 双轨                    | 非法状态转换拦截                                            | P0     |
| **CAP-GEN-006**  | Beat 粒度分段正文生成与流式输出               | AiGeneratePanel / 写作工作台                               | `useGenerationStreamPreview` → `chapterProseOrchestrator.generateChapterProse()` → `chapterSceneGenerationExecutionService` → `aiExecutionPipeline` → Tauri Stream                                                        | In: `ScenePlan, Context`<br>Out: `Stream<TextChunk>`              | 否           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 Workbench DSH chapter_writer 重叠           | 局部超时熔断与前缀复用                                      | P0     |
| **CAP-GEN-007**  | 生成后质量门禁自动裁判 (Score >= 80)          | 正文生成完成后自动触发                                     | `qualityGateRunner.evaluate()` → `chapterQualityGateService.evaluateQuality()`                                                                                                                                            | In: `proseText, constraints`<br>Out: `QualityGateVerdict`         | 否           | 否           | 是                  | **WORKING**              | 是       | 是 (SubAgent)   | 与 CheckPanel 重叠                             | 低分触发外部自动救援                                        | P0     |
| **CAP-GEN-008**  | 正文多轮迭代修订引擎                          | 生成后不满意触发重写                                       | `chapterRevisionService.reviseChapterDraft()` → `aiExecutionPipeline` → AI Model API                                                                                                                                      | In: `draftText, instructions`<br>Out: `RevisedProse`              | 否           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 PolishPanel 重叠                            | 修订漂移过度修改原意                                        | P1     |
| **CAP-QLT-001**  | 全文多维度 AI 质量审计报告                    | RightDock CheckPanel                                       | `CheckPanel` → `qualityCheckService.runQualityCheck()` → `qualityCheckAiService` → `invoke('create_quality_check_report'/'save_quality_check_result')` → SQLite `quality_check_reports/items`                             | In: `draftId, content`<br>Out: `QualityReport`                    | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 AgentTool check_quality 重叠                | 报告快照不可变保护                                          | P0     |
| **CAP-QLT-002**  | 质量问题状态流转跟踪 (Ignored/Fixed/Resolved) | CheckPanel 问题列表项                                      | `CheckPanel` → `qualityCheckService.updateIssueStatus()` → `invoke('update_quality_issue_status')` → SQLite `quality_issue_states`                                                                                        | In: `issueKey, status`<br>Out: `void`                             | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 跨报告历史状态合并                                          | P1     |
| **CAP-QLT-003**  | 质量问题正文精确定位                          | CheckPanel 点击问题项                                      | `CheckPanel` → `useEditorLocateTarget.locateIssueRange()` → `EditorArea.setSelectionRange()`                                                                                                                              | In: `startOffset, endOffset`<br>Out: `void (UI Scroll)`           | 否           | 否           | 是                  | **WORKING**              | 否       | 否              | 纯 UI 交互                                     | 正文编辑后偏移量失效                                        | P1     |
| **CAP-QLT-004**  | AI 风格润色 (流畅/描写/张力/对白/情绪)        | RightDock PolishPanel                                      | `PolishPanel` → `polishService.runPolish()` → `polishAiService` → SQLite `polish_records`                                                                                                                                 | In: `mode, selectionText, prompt`<br>Out: `PolishedText`          | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 DSH polish_chapter 重叠                     | 模式提示词溢出                                              | P1     |
| **CAP-QLT-005**  | 质量问题确定性范围替换闭环修复                | CheckPanel "一键修复"                                      | `CheckPanel` → `qualityFixService.runQualityFix()` → `qualityFixRangeApplication` (模糊匹配消歧与安全落盘) → SQLite `quality_fix_runs`                                                                                    | In: `reportId, issues, draftId`<br>Out: `QualityFixResult`        | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 无                                             | 替换区间重叠与幻觉文本拒收                                  | P0     |
| **CAP-MEM-001**  | 上下文记录 CRUD 与启停状态                    | RightDock ContextViewPanel / 作品详情                      | `ContextViewPanel` → `contextRecordService` → `invoke('save_context_records'/'get_context_records'/'update_context_record_active')` → SQLite `context_records`                                                            | In: `ContextRecordInput`<br>Out: `ContextRecord`                  | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 novelMemoryManager 重叠                     | 重要度排序截断                                              | P1     |
| **CAP-MEM-002**  | 章节总结生成与上下文沉淀                      | RightDock ChapterSummaryPanel                              | `ChapterSummaryPanel` → `chapterSummaryService` → `chapterSummarizeService` → `invoke('save_chapter_summary')` → SQLite `chapter_summaries`                                                                               | In: `chapterId, adoptedDraft`<br>Out: `ChapterSummary`            | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 DSH summarize_chapter 重叠                  | 总结信息遗漏关键伏笔                                        | P0     |
| **CAP-MEM-003**  | 分卷宏观总结提炼                              | 上下文压缩管线                                             | `volumeSummaryService.generateVolumeSummary()` → `aiExecutionPipeline` → AI Model API                                                                                                                                     | In: `volumeId, summaries`<br>Out: `VolumeSummary`                 | 否           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 无                                             | Token 预算过载                                              | P1     |
| **CAP-MEM-004**  | 角色库 CRUD 与动态心境/伤势跟踪               | RightDock CharactersPanel / 作品详情                       | `CharactersPanel` → `characterService` → `invoke('list_characters'/'save_character_state'/'get_character_states_by_character')` → SQLite `characters/states`                                                              | In: `CharacterInput, State`<br>Out: `Character`                   | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 Protagonist 双写                            | 角色状态版本冲突                                            | P0     |
| **CAP-MEM-005**  | 章节登场人物与出场要求绑定                    | CharactersPanel 出场勾选                                   | `CharactersPanel` → `characterService` → `invoke('add_chapter_character'/'remove_chapter_character')` → SQLite `chapter_characters`                                                                                       | In: `chapterId, characterId, mustAppear`<br>Out: `void`           | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 联合唯一索引保护                                            | P1     |
| **CAP-MEM-006**  | 章节事件设计与状态机 (Candidate/Planned/Done) | RightDock EventsPanel                                      | `EventsPanel` → `chapterEventService` → `invoke('list_chapter_events'/'create_chapter_event'/'set_chapter_event_status')` → SQLite `chapter_events`                                                                       | In: `ChapterEventInput`<br>Out: `ChapterEvent`                    | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 状态非法回退                                                | P1     |
| **CAP-MEM-007**  | AI 角色与剧情事件自动推演生成                 | CharactersPanel / EventsPanel AI 按钮                      | `CharactersPanel` → `characterGenerateService` / `eventSuggestService` → `aiExecutionPipeline` → AI Model API                                                                                                             | In: `ContextInfo`<br>Out: `GeneratedEntities`                     | 否           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 DSH generate_characters/suggest_events 重叠 | 角色同质化                                                  | P1     |
| **CAP-MEM-008**  | 设定推演与世界观扩写建议                      | SettingSuggestionsPage (`/novels/:id/setting-suggestions`) | `SettingSuggestionsPage` → `settingSuggestionService` → `settingExpandService` → `invoke('save_world_setting')`                                                                                                           | In: `novelId, currentSettings`<br>Out: `SettingSuggestion[]`      | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 DSH expand_settings 重叠                    | 冲突设定采纳                                                | P1     |
| **CAP-MEM-009**  | 混合语义记忆检索 (FTS5 全文 + 向量 Embedding) | MemoryInspectorPanel / Agent Tools                         | `MemoryInspectorPanel` → `memoryService.retrieve()` → `invoke('retrieve_memory')` → Rust `memory_repo` (SQLite FTS5 + cosine similarity)                                                                                  | In: `novelId, query, tokenBudget`<br>Out: `MemoryRetrievalResult` | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 novelMemoryManager 重叠                     | 无 Embedding 时自动降级 FTS5                                | P0     |
| **CAP-MEM-010**  | 上下文动态压缩与 Delta 状态回滚               | Workbench 压缩面板                                         | `useWorkbenchCompression` → `novelContextCompressionProvider.compress()` → `contextRecordService` (版本快照)                                                                                                              | In: `novelId, conversationId`<br>Out: `CompressionCard`           | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 压缩丢失重要实体                                            | P1     |
| **CAP-MAG-001**  | 多专家角色配置 (剧情/人物/文风/世界观)        | RightDock MultiAgentPanel                                  | `MultiAgentPanel` → `expertRegistry.ts`                                                                                                                                                                                   | In: `ExpertSelection`<br>Out: `ExpertConfig[]`                    | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 专家权重分配                                                | P1     |
| **CAP-MAG-002**  | 多专家并发独立评审会话                        | MultiAgentPanel "启动评审"                                 | `MultiAgentPanelRuntime` → `multiAgentService.runReviewSession()` → `invoke('create_multi_agent_session'/'append_multi_agent_round')` → SQLite `multi_agent_sessions/rounds/opinions`                                     | In: `draftText, roundCount, quorum`<br>Out: `MultiAgentSession`   | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 无                                             | 单专家超时不影响 Quorum                                     | P1     |
| **CAP-MAG-003**  | 多专家意见仲裁与迭代改写闭环                  | MultiAgentPanel 意见展示与共识仲裁                         | `MultiAgentPanel` → `multiAgentService.calculateConsensus()` → (Accept / Revise / Regenerate) → Auto Create Draft Candidate                                                                                               | In: `SessionOpinions`<br>Out: `ConsensusVerdict`                  | 是           | 否           | 是                  | **WORKING**              | 是       | 是 (SubAgent)   | 无                                             | 达到最大轮数未达成共识退出                                  | P1     |
| **CAP-AUT-001**  | 百万字全书多卷大纲规划生成                    | AutonomousPlanningPage (`/novels/:id/autonomous-planning`) | `AutonomousPlanningPage` → `autonomousStoryService.generatePlan()` → `invoke('save_autonomous_story_plan')` → SQLite `autonomous_story_plans`                                                                             | In: `StoryBrief`<br>Out: `AutonomousStoryPlan`                    | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 无                                             | 结构解析失败                                                | P1     |
| **CAP-AUT-002**  | 3 模式调度器生命周期 (全自动/半自动/人工)     | AutonomousPlanningPage 调度控制面板                        | `AutonomousSchedulerControls` → `autonomousSchedulerService` → `invoke('create_autonomous_book_run'/'acquire_autonomous_run_lease'/'pause'/'resume'/'stop')` → SQLite `autonomous_book_runs`                              | In: `runId, mode, budget`<br>Out: `SchedulerStatus`               | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 跨进程分布式租约防重                                        | P1     |
| **CAP-AUT-003**  | 自动章节生成/质检/晋升流水线                  | AutonomousPlanningPage 后台任务                            | `AutonomousPlanProgress` → `autonomousSchedulerWorker` → `claim_autonomous_run_chapter` → `finish_autonomous_run_chapter` → `promote_autonomous_run_attempt`                                                              | In: `chapterId`<br>Out: `AttemptRecord`                           | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 Workbench 流程重叠                          | 预算耗尽自动挂起                                            | P1     |
| **CAP-WKB-001**  | 对话工作台会话与轮次管理                      | WorkbenchPage (`/`) 左侧任务树与对话区                     | `useWorkbenchConversations` → `taskConversationService` → `invoke('create_task_conversation'/'append_conversation_turn')` → SQLite `task_conversations/turns`                                                             | In: `novelId, modelSnapshot`<br>Out: `TaskConversation`           | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 AgentConversationService 重叠               | 对话轮次时序断裂                                            | P0     |
| **CAP-WKB-002**  | 任务执行 Run 与工具调用事件追踪               | WorkbenchPage 消息流与工具行                               | `useWorkbenchTaskRunner` → `taskConversationService` → `invoke('create_task_run'/'append_tool_call_event'/'update_tool_call_event')` → SQLite `task_runs/tool_call_events`                                                | In: `runId, toolEvent`<br>Out: `TaskRun`                          | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 generation_jobs 双轨                        | 并发锁只允许 1 个 active run                                | P0     |
| **CAP-WKB-003**  | 产物卡片渲染与审阅授权流转                    | WorkbenchPage 产物卡片区                                   | `ArtifactCard` → `artifactDecisionService` → `invoke('create_conversation_artifact_card'/'record_artifact_decision'/'issue_review_authorization')` → SQLite `conversation_artifact_cards/decisions/review_authorizations` | In: `candidateArtifact, decision`<br>Out: `ReviewAuthToken`       | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 placement_proposals 双轨                    | 授权一次性消费保护                                          | P0     |
| **CAP-WKB-004**  | 候选产物结构化落盘应用 (章/角色/事件/设定)    | ArtifactCard "确认应用" 按钮                               | `ArtifactCard` → `artifactApply.applyArtifactCandidate()` → Domain Services (`draftVersionService`, `characterService`, etc.)                                                                                             | In: `cardId, candidatePayload`<br>Out: `ApplyResult`              | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 content_transactions 重叠                   | 正文草稿覆写安全提示                                        | P0     |
| **CAP-WKB-005**  | DSH 章节准备度分析与检查摘要                  | WorkbenchPage DshPreparationCard                           | `useDshPreparation` → `invoke('dsh_prepare_chapter'/'get_dsh_preparation_summary')` → SQLite `dsh_preparation_runs`                                                                                                       | In: `novelId, chapterId`<br>Out: `DshPreparationSummary`          | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 ChapterReadinessPlanCard 重叠               | 缺失项红黄牌警示                                            | P0     |
| **CAP-WKB-006**  | DSH 任务服务器托管与多轮协议                  | WorkbenchPage "发送任务"                                   | `useWorkbenchTaskRunner` → `taskRuntimeAdapter.startTurn()` → `invoke('dsh_start_task_turn'/'dsh_cancel_task_run')` → Rust DSH Child Process                                                                              | In: `prompt, allowedTools`<br>Out: `Stream<TurnEvents>`           | 是           | 是           | 是                  | **WORKING**              | 否       | 是 (SubAgent)   | 与 AgentLoop 重叠                              | 进程超时崩溃自动隔离恢复                                    | P0     |
| **CAP-WKB-007**  | 生产工具注册表与候选结构校验槽                | Workbench 工具执行派发                                     | `productionToolRegistry.ts` → `ToolRegistry.invoke()` → `validateCandidateText()`                                                                                                                                         | In: `toolName, argumentsJson`<br>Out: `ToolResult`                | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 AgentToolRegistry 重叠                      | 契约格式不匹配 fail-closed                                  | P0     |
| **CAP-SYS-001**  | AI 任务全量历史审计记录与分页                 | AiTasksPage (`/ai-tasks`) / GenerationTracePanel           | `AiTasksPage` → `aiTaskService` → `invoke('get_ai_task_records'/'count_ai_task_records'/'delete_ai_task_record')` → SQLite `ai_task_records`                                                                              | In: `page, size, filters`<br>Out: `{ items, total }`              | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 ai_tasks_v2 双轨                            | 500 条限制已升级为服务端分页                                | P1     |
| **CAP-SYS-002**  | 不可变 AI 结果 Artifact 与校验问题库          | 底层 AI 执行管线                                           | `invoke('create_result_artifact'/'get_result_artifact'/'list_result_artifacts_for_task')` → SQLite `result_artifacts_v2/issues`                                                                                           | In: `attemptId, artifactPayload`<br>Out: `ResultArtifact`         | 是           | 否           | 是                  | **WORKING**              | 否       | 否              | 无                                             | 强哈希校验与不可变追加                                      | P1     |
| **CAP-SYS-003**  | 全局 AI 请求治理与并发/Token/成本预算         | SettingsPage (`/settings`) AI 治理卡片                     | `AiGovernanceSettingsCard` → `aiRequestPolicyService` → `invoke('configure_ai_request_policy'/'reserve_ai_request'/'settle_ai_request')` → SQLite `global_ai_request_policies`                                            | In: `PolicyConfig, RequestParams`<br>Out: `ReservationLease`      | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 额度超限立即阻断请求                                        | P0     |
| **CAP-SYS-004**  | 本地章节大模型健康巡检与场景协议              | SettingsPage 本地模型卡片                                  | `LocalChapterModelSettingsCard` → `localChapterModelHealthService` → `invoke('check_local_chapter_model')` → Loopback HTTP `/health`                                                                                      | In: `endpoint, modelName`<br>Out: `ModelHealthStatus`             | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 非 Loopback 地址安全拒绝                                    | P0     |
| **CAP-SYS-005**  | 远程 Writer 节点调度与 Beat 协议策略          | SettingsPage 远程 Writer 卡片                              | `RemoteWriterSettingsCard` → `aiSettingsService` + `remoteWriterNetworkPolicy.ts`                                                                                                                                         | In: `RemoteWriterConfig`<br>Out: `void`                           | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 离线自动优雅降级                                            | P1     |
| **CAP-SYS-006**  | 项目全量备份导出与无损恢复                    | SettingsPage 数据存储卡片                                  | `DataStorageSettingsCard` → `projectBackupService` → `invoke('export_project_backup'/'import_project_backup')` → Rust `project_backup.rs`                                                                                 | In: `filePath \| backupJson`<br>Out: `BackupMetadata`             | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 exportService.exportBackup 重叠             | 备份哈希篡改即拒绝写入                                      | P0     |
| **CAP-SYS-007**  | 单章与全书 TXT / Markdown 导出                | ImportExportPage (`/import-export`)                        | `ImportExportPage` → `exportService.exportChapterToTxt/Markdown/exportNovelToTxt` → Tauri Dialog / FS                                                                                                                     | In: `novelId \| chapterId`<br>Out: `savedFilePath`                | 否           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 未采用正文无法导出提醒                                      | P1     |
| **CAP-SYS-008**  | TXT / JSON 外部小说作品导入                   | ImportExportPage 导入弹窗                                  | `ImportExportPage` → `ImportTxtDialog` / `ImportJsonDialog` → `jsonImportService` / `txtImportService` → `novelRepository.create()`                                                                                       | In: `fileContent, parseRule`<br>Out: `Novel`                      | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 与 reference_library 格式解析重叠              | 章节正则表达式切分容错                                      | P1     |
| **CAP-SYS-009**  | 客户端版本更新检测与原生安装                  | SettingsPage 软件更新卡片                                  | `AppUpdateSettingsCard` → `appUpdateService` → `invoke('check_app_update'/'install_app_update')` → Tauri Updater                                                                                                          | In: `channel`<br>Out: `UpdateInfo`                                | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 网络受限提示                                                | P2     |
| **CAP-SYS-010**  | 原生崩溃拦截与诊断日志报告                    | SettingsPage 诊断日志卡片                                  | `DiagnosticsSettingsCard` → `nativeCrashReportService` → `invoke('get_native_crash_reports'/'clear_native_crash_reports')` → Rust hook                                                                                    | In: 无<br>Out: `CrashReport[]`                                    | 是           | 否           | 是                  | **WORKING**              | 是       | 否              | 无                                             | 敏感凭据脱敏                                                | P1     |
| **CAP-SYS-011**  | 主题切换与 Windows 系统主题色同步             | SettingsPage 外观设置卡片                                  | `AppearanceSettingsCard` → `themeRuntimeService` → `invoke('get_system_accent_color')` → Windows Registry / CSS Vars                                                                                                      | In: `themeMode`<br>Out: `accentHex`                               | 是           | 否           | 是                  | **WORKING**              | 否       | 否              | 纯 UI 交互                                     | 注册表查询受限回退默认色                                    | P2     |
| **CAP-SYS-012**  | 单实例锁与窗口位置记忆                        | 桌面应用启动与关闭                                         | Rust `main.rs` → `window_state.rs` → `try_acquire_instance_lock` + `load_window_state`                                                                                                                                    | In: `appDataDir`<br>Out: `WindowState`                            | 是           | 否           | 是                  | **WORKING**              | 否       | 否              | 纯底层桌面壳                                   | 多实例并发启动冲突重定向                                    | P0     |
| **CAP-LEG-001**  | 用户模板本 (仅 LocalStorage)                  | TemplatesPage (`/templates`)                               | `TemplatesPage` → `templateService` (`src/services/templates/templateService.ts`) → `localStorage.getItem('ai_novel_studio_user_templates')`                                                                              | In: `UserTemplate`<br>Out: `UserTemplate[]`                       | 是 (仅LS)    | 否           | 是                  | **PARTIAL**              | 否       | 否              | 与 prompts/ 模板库脱节                         | **完全未存 SQLite，也未接入生成管线**                       | P2     |
| **CAP-LEG-002**  | 创作资产导航门户页                            | AssetsPage (`/assets`)                                     | `AssetsPage` → 聚合读取 `novelRepository, characterService, chapterSummaryService, contextRecordService`                                                                                                                  | In: `selectedNovelId`<br>Out: `StatsCounters`                     | 否           | 否           | 是                  | **WORKING** (Portal)     | 否       | 否              | 无                                             | 仅做跳转导航，无自身独立数据                                | P2     |
| **CAP-LEG-003**  | 施工中占位页                                  | ComingSoonPage (`/coming-soon`)                            | 路由占位，当前无主菜单直接导航指向                                                                                                                                                                                        | In: `fromParam`<br>Out: `PlaceholderUI`                           | 否           | 否           | 否                  | **SHELL**                | 否       | 否              | 无                                             | 无实际业务功能                                              | P3     |
| **CAP-LEG-004**  | 早期静态 Workflow 生成器 (Planner Lite)       | 无 UI 入口 (`src/agent/planner-lite.ts`)                   | `src/agent/planner-lite.ts` (`createChapterGenerationWorkflow`, `createChapterReadinessWorkflow`)                                                                                                                         | In: `PlannerInput`<br>Out: `StaticMockWorkflow`                   | 否           | 否           | 否                  | **BROKEN / ORPHANED**    | 否       | 否              | 与 agentPlanRuntimeService 重复                | **早期原型文件，全工程 0 处引用**                           | P3     |
| **CAP-LEG-005**  | 内存级 Agent 创作循环与聊天工作区             | `src/features/agent/AgentChatWorkspace.tsx` (仅有测试)     | `agentConversationService` → `CreativeAgentHarness` (`src/services/agent/agentLoop.ts`) → `AgentToolRegistry` → `novelMemoryManager`                                                                                      | In: `userInput`<br>Out: `AgentExecutionResult`                    | 否 (内存)    | 是           | 否 (主UI未连)       | **PARTIAL / DUPLICATE**  | 否       | 是 (可重构核心) | 与 Workbench DSH 对话工作台双轨平行            | **主界面 `/` 走 WorkbenchPage，本组件未挂载到任何真实路由** | P1     |
| **CAP-LEG-006**  | 单目标 Placement 提议与应用计划 (v2.0)        | `placementRuntimeService.ts`                               | `placementRuntimeService` → `invoke('prepare_placement_proposal'/'apply_placement_plan')` → SQLite `placement_proposals_v1`                                                                                               | In: `proposalPayload`<br>Out: `ApplyPlan`                         | 是           | 否           | 降级备用            | **PARTIAL / SUPERSEDED** | 否       | 否              | 被 Migration 036 artifactDecisions 替代        | 仅支持单目标，不支持跨章事务                                | P2     |
| **CAP-LEG-007**  | Agent 计划任务与步骤租约调度 (v2.0)           | ChapterReadinessPlanCard (`useChapterReadinessPlan.ts`)    | `useChapterReadinessPlan` → `agentPlanRuntimeService` → `invoke('create_agent_plan'/'claim_agent_plan_step')` → SQLite `agent_plans_v1`                                                                                   | In: `chapterId`<br>Out: `AgentPlan`                               | 是           | 否           | 是                  | **PARTIAL**              | 否       | 否              | 与 DSH Task Runner 双轨                        | 仅在准备度检查卡片中触发                                    | P2     |
| **CAP-LEG-008**  | 内存级小说动态状态与记忆管理器                | `src/services/memory/novelMemoryManager.ts`                | `novelMemoryManager` (内存 Map + Snapshot + In-memory Vector Fallback)                                                                                                                                                    | In: `MemoryDelta`<br>Out: `MemorySnapshot`                        | 否 (仅内存)  | 否           | 仅 Harness 测例使用 | **PARTIAL / DUPLICATE**  | 否       | 否              | 与 SQLite `memoryService` (Migration 026) 重叠 | 数据随进程重启丢失，未与 SQLite 同步                        | P2     |
