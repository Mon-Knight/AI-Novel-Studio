---
description: AI Agent 安全规则 - 禁止自动扩展需求、禁止大规模重构、禁止删除已有功能
globs: **/*
alwaysApply: true
---

# Agent Safety Rules

## 核心原则

AI Agent 在操作本仓库时，必须遵循 **安全优先** 原则。

## 绝对红线（无论如何不得违反）

| 禁止行为 | 说明 |
|----------|------|
| 自动扩展需求 | 用户说 A，不能自己加上 B、C、D |
| 大规模重构 | 不得一次性重写整个项目或多个模块 |
| 自行新增未来功能 | 不得实现当前版本任务书中没有的功能 |
| 删除已有功能 | 不得删除用户未要求删除的页面/路由/功能 |
| 修改无关模块 | 不得顺手"优化"与任务无关的代码 |
| 修改数据库结构 | 除非版本任务明确要求 |
| 更换技术栈 | 绝对不得自行决定 |
| 提交敏感信息 | API Key / 密码 / .env.local |

## 修改约束

- 一次只修改一个明确目标相关的文件
- 修改前必须分析影响范围
- 修改后必须运行验证
- 不顺手重构"看起来不好"的旧代码
- 不引入未要求的依赖

## 工作流要求

```
分析 → 规划 → 执行 → 验证
```

禁止"边想边改"模式。

## 与用户沟通

以下情况必须询问用户：
- 需求不明确
- 需要架构决策
- 需要新依赖
- 影响多个模块
- 发现潜在风险
