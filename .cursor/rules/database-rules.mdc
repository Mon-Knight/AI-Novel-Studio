---
description: AI Novel Studio 数据库规则 - Schema 安全、迁移约束、Agent 预留
globs: src-tauri/src/**/*.rs,src/services/database/**/*
alwaysApply: true
---

# Database Rules

## 数据库安全第一

AI Novel Studio 存储用户全部创作资产。数据库操作必须安全、向后兼容、可回退。

## Schema 修改规则

### 绝对禁止
- ❌ 删除字段 → 改为标记 deprecated
- ❌ 重命名字段 → 改为新增 + 迁移
- ❌ 修改字段类型 → 改为新增正确类型字段
- ❌ 删除表 → 改为标记不再使用
- ❌ 生产代码中使用 DROP TABLE

### 允许的操作
- ✅ ALTER TABLE ADD COLUMN（必须有默认值）
- ✅ CREATE TABLE IF NOT EXISTS
- ✅ CREATE INDEX IF NOT EXISTS
- ✅ CREATE VIEW IF NOT EXISTS

## 查询规范

- 必须使用参数化查询，禁止字符串拼接 SQL
- 多表操作必须使用事务（BEGIN/COMMIT/ROLLBACK）
- 频繁查询列必须建立索引
- 大文本字段考虑分离存储

## Agent 系统预留

现有表应考虑预留字段：
- `agent_status` - Agent 处理状态
- `agent_metadata` - Agent 元数据 (JSON)
- `task_id` - 关联 Agent 任务 ID
- `version` - 数据版本号

未来可能新增的表（预留命名空间）：
- `agent_tasks`
- `agent_memory`
- `tool_calls`
- `plans`

## 数据库文件

- Tauri 模式：%APPDATA%/ai-novel-studio/
- 不提交 .db / .sqlite 文件到 Git
