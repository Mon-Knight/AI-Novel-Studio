---
description: AI Novel Studio 测试规则 - 必须运行的验证命令、验证清单
globs: **/*
alwaysApply: true
---

# Testing Rules

## 验证分层

### 纯文档

```powershell
npm run test:docs-sync
npm run test:version-sync   # 涉及版本/路线时
npx prettier --check <changed-docs>
git diff --check
```

### 前端 / TypeScript

```powershell
<相关动态测试>
npm run lint:ci
npm run build
```

### Rust / SQLite

```powershell
cd src-tauri
cargo check
<相关 cargo test>
cd ..
```

### 发布、Tauri 或 DSH 载体

```powershell
powershell -ExecutionPolicy Bypass -File scripts/agent-workflow/verify_project.ps1
```

完整桌面 E2E、Tauri 构建和 clean working tree 是发布门禁；纯文档或局部代码任务按影响范围验证，不伪造无关门禁结果。

## 验证检查清单

- [ ] 与改动直接相关的动态测试通过
- [ ] 前端改动的 lint/build 通过
- [ ] Rust 改动的 cargo check/相关测试通过
- [ ] 发布任务的统一验证入口通过且工作树 clean
- [ ] v3.3.0+ 创作工作台或原章节审阅编辑器按目标版本正常渲染
- [ ] 无控制台报错

## 构建失败处理

1. 完整记录错误输出
2. 定位失败文件和行号
3. 分析原因
4. 修复后重新运行全部验证
5. 全部通过后才可提交

## 禁止

- ❌ 跳过验证步骤
- ❌ 忽略构建警告/错误
- ❌ 提交未通过验证的代码
