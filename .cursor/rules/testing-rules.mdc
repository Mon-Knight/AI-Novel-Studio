---
description: AI Novel Studio 测试规则 - 按变更范围选择验证、专项与发布入口、失败处理
globs: src/**/*.test.ts,src/**/*.test.tsx,src/**/*.test.mjs,src/test/**/*,tests/**/*,scripts/quality/**/*,scripts/agent-workflow/**/*,scripts/e2e/**/*,vitest*.ts,.github/workflows/**/*
alwaysApply: false
---

# Testing Rules

本文细化根 `AGENTS.md` 第 6 节的验证矩阵，只运行变更范围适用的门禁，不能反向扩大任务。

## 三类入口

| 层级                | 入口                                                                                  |
| ------------------- | ------------------------------------------------------------------------------------- |
| 日常修改            | `npm run verify:change`（先 `-- --dry-run` 查看选择理由）                             |
| 专项验收            | 专项脚本或 `npm run test:e2e -- --spec <a> --spec <b>`（一次构建、多个场景）          |
| 发布 / 明确完整验收 | `powershell -ExecutionPolicy Bypass -File scripts/agent-workflow/verify_project.ps1` |

## 选择器规则

- 变更路径 → 行为测试 / Rust 筛选 / 桌面场景的归属表在 `scripts/quality/verification-scopes.mjs`；新模块必须声明归属，未映射路径与零测试都失败关闭，不自动扩大为全仓检查。
- 局部前端：相邻或所属模块行为测试、改动文件 ESLint、一次类型检查；用户交互/写作流程变化加对应真实桌面场景。
- Rust/SQLite：`cargo check --locked --manifest-path src-tauri/Cargo.toml` 与有非零匹配证明的相关 Rust 测试（`scripts/quality/run-cargo-tests.mjs`，保留 `--locked` 与 `--test-threads=1`）。
- Migration、共享持久化、DSH、打包配置、CI 工作流：扩大到对应完整领域门禁；打包变化验证 `npm run tauri:build`。DSH 先准备固定载体与当前 Gateway，不使用不明来源缓存。
- 同一批未变化且已通过的检查不重复运行；失败修复或具体新风险只复测受影响项。覆盖率在首次执行采集，报告阶段不重跑测试。

## 桌面验收

默认生产界面、真实 Tauri、隔离 SQLite、固定模型响应；旧面板只由 `scripts/e2e/spec-selection.ts` 列出的兼容性用例显式启用。`workbench-writing-smoke` 是日常写作场景（含真实进程重启与 SQLite 核对），五轮跨作品闭环用于完整验收。Mock 总结失败是失败边界证据，不得写成总结成功。

## 失败处理

1. 完整记录命令、退出码与首个根因
2. 区分本次引入、既有问题与环境缺失；既有问题如实报告，不借此扩大任务
3. 修复本次引入的问题后只复测受影响项
4. `NOT_RUN`、`NOT_APPLICABLE`、Mock、浏览器与真实桌面/live 证据分别报告；报告限制不等于豁免发布门禁

## 禁止

- ❌ 跳过选择器选出的检查，或把零用例、缺工具写成通过
- ❌ 忽略构建警告/错误
- ❌ 为普通修改运行完整发布矩阵，或失败后无条件重跑全部门禁
- ❌ 提交未通过验证的代码
